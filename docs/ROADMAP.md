# Roadmap (tiny-step mode)

## Next 3
1) **Site-aware roster**  
   - Add `site: "FD"|"DK"`, implement DK MLB roster (2P, C/1B, 2B, 3B, SS, OF×3), auto default caps per site, UI dropdown.
2) **Contest config → smart defaults**  
   - Add `contest_size`, `entries`; auto-suggest pool_size, n_sims, field_size.
3) **Filter & prune UI on /runs**  
   - Min proj, salary range, min Top10%, team stack filter; include/exclude players; export filtered set (with uniques).

## Later
- Real FD/DK export formats.
- Avoid hitter-vs-pitcher (needs matchups).
- More realistic field via ownership-weighted field pool.
- Presets + seed control.
